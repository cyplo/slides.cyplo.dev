{
  "name": "Cyryl's slides",
  "version": "0.0.0",
  "author": "Cyryl Płotnicki",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "npm run -s og-image && npm run -s deck",
    "deck": "marp --no-stdin PITCHME.md -o dist/index.html",
    "now-build": "npm run -s deck",
    "og-image": "marp PITCHME.md -o dist/og-image.jpg",
    "start": "marp -ps ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^0.9.2"
  }
}